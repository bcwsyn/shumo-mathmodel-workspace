#let three-line-table(caption, columns, header, body) = {
  let col-count = header.len()
  let bottom-y = calc.floor(body.len() / col-count) + 1
  let styled-header = header.map(cell => strong(cell))
  block(width: 100%, breakable: false)[
    #align(center)[
      #box[
        #align(center)[#text(size: 10.5pt, weight: "bold")[#caption]]
        #v(0.6em)
        #table(
          columns: columns,
          align: center,
          stroke: none,
          inset: (x: 0.35em, y: 0.52em),
          table.hline(y: 0, stroke: 0.8pt),
          table.hline(y: 1, stroke: 0.5pt),
          table.hline(y: bottom-y, stroke: 0.8pt),
          ..styled-header,
          ..body,
        )
      ]
    ]
  ]
}

= 问题重述与总体思路

企业需要在零配件采购、装配、检测、拆解和售后调换之间作出联动决策。检测可以阻止缺陷继续向下游传播，却会增加逐件成本；拆解能够回收上游投入，却可能形成重复装配循环。本文以一名客户最终获得合格产品时企业的期望净收益为统一指标，先从抽样中获得缺陷率信息，再将条件缺陷传播、检测费用、报废损失与回收循环纳入同一更新报酬模型。

四问形成递进关系：问题一给出供应批接收与拒收规则；问题二在两零件单工序中精确枚举策略；问题三把同一更新逻辑推广到树形生产网络；问题四把点缺陷率替换为抽样分布，考察推荐策略是否稳定。

= 模型假设与符号

假设零配件缺陷与条件装配缺陷相互独立，检测准确且不损伤合格件，拆解不损伤可回收的直接输入。若未检测的坏件被回收且在既定策略下永远无法被识别，则对应循环不吸收，判为不可行。上述假设使题面给出的条件次品率能够逐层组合；相关性、检测误差与拆解损伤将在模型评价中作为局限。

记节点缺陷率为 $p_i$，采购或装配成本为 $c_i$，检测费为 $d_i$，拆解费为 $h_i$，售价与额外调换损失为 $r, ell$。对策略 $pi$，以 $V_pi$ 表示从当前生产状态到完成一件合格交付品的期望净收益。所有货币单位均为元/件。

= 问题一：抽样检测

== 精确置信判定

对样本量 $n$ 和次品数 $x$，使用单侧 Clopper--Pearson 界#super("[2]")。95% 单侧下界为
$ L_0.95(x,n)="Beta"^(-1)(0.05;x,n-x+1), $
90% 单侧上界为
$ U_0.90(x,n)="Beta"^(-1)(0.90;x+1,n-x). $
当 $L_0.95>p_0$ 时以 95% 信度拒收；当 $U_0.90<=p_0$ 时以 90% 信度接收；其余结果继续抽样。标称值 $p_0=0.10$ 时，最有利极端观察的数学下限如下。

#three-line-table(
  [表 1  精确单侧置信判定的边界样本],
  (1fr, 1.2fr, 1.2fr, 1.4fr),
  ([样本], [95% 下界], [90% 上界], [结论]),
  ([22 件全合格], [0], [9.9372%], [接收],
   [2 件全为次品], [22.3607%], [1], [拒收]),
)

这两个边界并不是固定抽 22 件即可总能决策：若观察结果不够极端，仍需继续抽样。为减少远离临界区的期望检测量，引入工程无差异带。

== 序贯概率比检验

令 $p_A=p_0-delta=0.08$、$p_R=p_0+delta=0.12$，取第一类错误 $alpha=0.05$、第二类错误 $beta=0.10$。抽到第 $t$ 件且累计 $s_t$ 件次品时，Wald 序贯统计量#super("[1]")为
$ Z_t=s_t log(p_R/p_A)+(t-s_t)log((1-p_R)/(1-p_A)). $
当 $Z_t>=log 18$ 时拒收，当 $Z_t<=log(2/19)$ 时接收，否则继续抽样；达到 1000 件仍未越界则转入复检。固定种子下每个真实次品率模拟 10000 条序列，5%、8%、10%、12%、15% 情景的平均检测数分别为 103.51、241.03、365.91、261.12、130.60。临界值附近检测量最大，符合序贯检验的识别难度。

#figure(image("../../figures/sprt_operating_characteristics.pdf", width: 85%), caption: [SPRT 在不同真实次品率下的运行特性])

= 问题二：两零配件生产决策

== 状态报酬方程

若两零件都未经过筛除，新装配成品的合格概率为
$ q=(1-p_1)(1-p_2)(1-p_A). $
检测某零件后，取得一件合格零件的期望采购与检测成本为 $(c_i+d_i)/(1-p_i)$。为同时处理部分检测、产品检测、拆解和售后退换，本文把每种零件状态编码为“缺件、潜在次品、合格”，对两个零件形成九状态过程。对任一四元策略 $pi$，建立转移矩阵 $P_pi$ 与即时净收益 $g_pi$，求解马尔可夫报酬方程#super("[3]")
$ (I-P_pi)V_pi=g_pi. $
若从初始状态可达的 $I-P_pi$ 奇异或严重病态，则说明坏件在回收循环中无法被清除，该策略不可行。否则 $V_pi$ 的初始分量就是该策略的期望净收益。

== 六种情形的决策

枚举“零件 1 检测、零件 2 检测、成品检测、不合格品拆解”四个二元选择，共 16 种策略。表 2 给出六种情形的最优可行策略；“1/0”分别表示执行/不执行。

#three-line-table(
  [表 2  问题二的推荐策略与期望净收益],
  (0.5fr, 0.7fr, 0.7fr, 0.7fr, 0.7fr, 1.1fr),
  ([情形], [检零1], [检零2], [检成品], [拆解], [净收益]),
  ([1], [1], [1], [0], [1], [18.111111],
   [2], [1], [1], [0], [1], [12.000000],
   [3], [1], [1], [0], [1], [15.444444],
   [4], [1], [1], [1], [1], [14.750000],
   [5], [0], [1], [0], [0], [11.987654],
   [6], [0], [0], [0], [0], [21.678670]),
)

高调换损失使成品检测更有价值，低缺陷率和高拆解费则削弱检测、拆解的收益。对情形 1 将缺陷率、检测成本、拆解费与调换损失分别缩放至 0.8、1.0、1.2 倍，最优策略均不翻转，净收益范围为 17.000000--19.222222；其中检测成本和缺陷率的影响最大。

#figure(image("../../figures/problem2_sensitivity.pdf", width: 85%), caption: [情形 1 最优策略的单参数敏感性])

= 问题三：多工序生产网络

将图 1 表示为树形有向无环图，叶节点为 8 个零配件，中间节点为 3 个半成品，根节点为成品。为避免未检坏件在拆解后形成不可吸收循环，实例采用“零件和半成品均检测”的可解释候选族，再比较半成品拆解、成品检测和最终拆解三个二元选择。

设一个内部节点的合格输入取得成本为 $C$，装配与检测成本为 $a+d$，条件缺陷率为 $p$。若失败后拆解复用输入，则合格节点成本为
$ C+(a+d+p h)/(1-p). $
若失败后报废，则输入也必须重新取得，成本为
$ (C+a+d)/(1-p). $
这一区分保证报废分支不会漏计下一次尝试的上游投入。由叶到根递推 8 个候选后，最佳方案为：检测全部零件和半成品；半成品不合格时拆解复用零件；最终成品不检测；售后退回时拆解复用半成品。期望净收益为 60.222222，最差候选为 37.119342。

#figure(image("../../figures/problem3_policy_comparison.pdf", width: 85%), caption: [图 1 结构化候选策略的收益比较])

该结果是对透明候选族的精确最优，不等同于对所有节点级、库存级历史策略的全局穷举。若生产系统允许批量库存共享或对回收件重新分级，应把库存量与检测历史加入状态。

= 问题四：抽样不确定性下的重决策

== 后验传播方案

若节点 $j$ 抽检 $n_j$ 件并观察到 $x_j$ 件次品，采用弱信息 Jeffreys 更新
$ p_j | x_j,n_j ~ "Beta"(x_j+1/2,n_j-x_j+1/2). $
每次从各节点分布抽取一组缺陷率，再完整重算问题二的 16 个策略和问题三的 8 个候选。对每个策略报告后验平均净收益、5% 分位数和“在该次抽样参数下成为最优”的频率。

题面没有给出实际 $(x_j,n_j)$。为使计算可复现，本文设置两个透明样本规模情景：每节点 $n=100$ 或 500，并把题面次品率四舍五入为相应次品数。表 3 显示问题二风险中性推荐策略成为逐次最优的频率。

#three-line-table(
  [表 3  问题二推荐策略的后验稳定性],
  (0.7fr, 1.2fr, 1.2fr),
  ([情形], [$n=100$], [$n=500$]),
  ([1], [92.8%], [100.0%],
   [2], [100.0%], [100.0%],
   [3], [54.0%], [46.6%],
   [4], [100.0%], [100.0%],
   [5], [56.8%], [70.0%],
   [6], [61.8%], [78.8%]),
)

情形 1、2、4 的策略稳定；情形 3、5、6 接近策略切换边界，应优先扩大样本或并列给出备选。问题三在两种样本规模下均推荐“半成品拆解、成品不检、售后拆解”：$n=100$ 时后验平均净收益 59.048357、5% 分位数 54.163802、逐次最优频率 92.9%；$n=500$ 时相应指标为 59.973857、57.834260 和 100%。

#figure(image("../../figures/problem4_policy_stability.pdf", width: 85%), caption: [不同样本规模下问题二推荐策略的后验稳定性])

以上结果回答了“缺陷率来自抽样时如何重做决策”，但不冒充真实抽样结论。实际使用时只需把真实 $(x_j,n_j)$ 代入同一接口，即可替换本节情景。

= 模型评价与推广

模型的优点在于：抽样判定使用精确小样本界；生产决策完整保留报废、拆解与售后循环；不可吸收策略通过线性方程条件数显式识别；问题四同时给出平均收益、下行分位数与策略稳定性。代码对关键边界、失败循环和随机重复性均有测试。

局限主要有三点。第一，缺陷独立性忽略供应批内相关与装配交互；第二，检测被视为完全准确，未计假阳性和假阴性；第三，问题三为结构化候选族而非含库存状态的全局策略。后续可用分层 Beta-Binomial 模型处理批间异质性，用检测混淆矩阵修正状态转移，并以近似动态规划或强化学习求解大规模生产网络。
