# Preserve the large, editable temperature-field vector source through Git LFS.
projects/CUMCM2018A_HighTemperatureClothing/figures/q1_temperature_field.svg filter=lfs diff=lfs merge=lfs -text

# Generated PDF deliverables are binary artifacts; never run textual whitespace checks on them.
*.pdf binary
