# V8.1 教师意见全面落实修订设计

## 目标与边界

- 源稿：`C:\Users\HP\OneDrive\xwechat_files\wxid_2bguojz3wpwl22_5ea0\msg\file\2026-09\全篇证据约束优化版_V8.1.docx`。
- 源稿 SHA-256：`93A5854030AFF664340487035C60BE2997FE26520EE49A8C201BFA8B69C9FBD0`。
- 交付：另存 `全篇证据约束优化版_V9.0_教师意见全面落实版.docx`、对应 PDF、可编辑流程图源、AI 使用台账、逐项落实矩阵和实验复现记录。
- 不覆盖 V8.1；不虚构规则、AI 使用、参考文献、数据、参数、测试或模型结果。
- V8.1 的真实方法是 Logistic/XGBoost 概率融合 CRI、共同特征跨样本外推、保序利率响应、MILP 和有限情景 maximin；不得把旧 PDF 的 Elastic Net、Bootstrap 90% 分位数及其数值写入 V8.1。

## 已批准的修订

### 1. 格式与源文件

- 删除“目 录”及其条目，不以 PDF 作为编辑入口。
- 保留原有标题、摘要、章节、表格、页眉页脚、页码和引用结构；布局变化必须在渲染审查中解释。
- 所有图形使用可编辑 SVG/Draw.io 源；最终导出物不得带生成式 AI 水印。

### 2. 术语与创新边界

- 将“信用风险迁移/可迁移”统一改为“基于共同特征的跨样本外推”，并明确这不等同于领域自适应或迁移学习算法。
- CRI 仅作为排序、风险敞口和组合约束的风险量；不得称为已校准的企业违约概率或其置信上界。
- MILP 的贡献写为风险、利率响应和额度约束的一体化配置；解释额度角点解，不宣称新的整数规划算法。
- maximin 写为有限压力情景下的稳健配置/压力测试；CVaR、分布鲁棒、相关违约和连续不确定集仅作为未来改进。

### 3. 证据与重算

- 先定位与 V8.1 一致的输入、代码、参数和已有结果；以哈希、运行环境、随机种子和日志绑定复现实验。
- 复查概率融合、跨样本漂移、预算边际/角点、LGD 和压力强度敏感性、风险约束回代与可行性边界。
- 无法由真实代码和数据重算的论断改为“待核验”或“局限”，不得补造 Bootstrap 覆盖率、目标域个体准确率或分布鲁棒理论保证。

### 4. AI 使用披露

- 根据指导老师明确要求，在不暴露作者身份的前提下设置三处一致且真实的披露：图形来源说明、附录 AI 使用与人工复核说明、参考文献中的实际工具官方文档。
- 台账至少记录工具与版本/访问日期、用途、关键输入输出、采纳/修改/拒绝边界和人工复核责任。
- 图形说明必须区分“AI 辅助构思”与“人工矢量重绘”；方法、数据、参数、计算和结论均由可复现代码或人工核验支撑。

## 执行与验收

1. 对 V8.1 建立只读基线与工作副本，抽取 OOXML、文字、图表、字段和页眉页脚清单。
2. 绑定可复现计算资产后，形成逐项“教师意见—原文位置—证据/重算—修订位置—验收状态”矩阵。
3. 先改术语、目录、AI 披露和参考文献，再重绘与替换流程图；任何数值性修改均要有重算记录。
4. 用 LibreOffice 与 Word 导出进行渲染检查，逐页核查页码、页眉页脚、公式、表格、图形、引用和附录；同时检查 DOCX 包结构，防止页脚关系或图片关系回归。
5. 交付 V9.0 时给出来源哈希、输出哈希、页数、复现状态、尚未验证项及逐项落实矩阵。

## 明确不做

- 不把指导老师的要求伪装成已发现的官方竞赛规则。
- 不以“AI 降重”或检测分数作为质量证据。
- 不生成不可编辑、带水印或与论文方法不一致的流程图。
- 不在没有对应实证计算的情况下将成熟方法包装为原创算法。
