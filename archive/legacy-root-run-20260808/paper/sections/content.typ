= 问题重述与总体思路

企业需在零配件采购、装配、检测、拆解和售后调换之间作出决策。本文以最终合格交付品的期望净收益为目标：先从抽样中获得缺陷率信息，再将条件缺陷传播、检测成本和回收循环统一进入策略评价。

= 模型假设与符号

零配件和条件装配缺陷相互独立；检测准确且不损伤合格件；拆解不损伤直接输入。若未检坏件被回收而无法被识别，则该循环判为不可行。记节点缺陷率为 $p_i$，采购或装配成本为 $c_i$，检测费为 $d_i$，拆解费为 $h_i$，售价和调换损失为 $r, ell$。

= 问题一：抽样检测

对样本量 $n$ 和次品数 $x$，以单侧 Clopper--Pearson 界#super("[2]")判断：若 95% 下界超过标称值，则拒收；若 90% 上界不超过标称值，则接收。为降低远离临界区的检测量，令 $p_A=p_0-delta$、$p_R=p_0+delta$，累计对数似然比为#super("[1]")
$ Z_t=s_t log(p_R/p_A)+(t-s_t)log((1-p_R)/(1-p_A)). $
当 $Z_t >= log 18$ 拒收，当 $Z_t <= log(2/19)$ 接收。22 件全合格的上界为 9.9372%；5% 和 15% 情景的平均检测数分别为 103.51 和 130.60。

#figure(image("../../figures/sprt_operating_characteristics.pdf", width: 85%), caption: [SPRT 在不同真实次品率下的运行特性])

= 问题二：两零配件生产决策

新装配成品的合格概率为 $q=(1-p_1)(1-p_2)(1-p_A)$。对策略 $pi$，以状态转移矩阵 $P_pi$ 和即时收益 $g_pi$ 建立#super("[3]")
$ (I-P_pi)V_pi=g_pi. $
该递推完整计入重新采购、检测、拆解和售后调换。表 1 的六种情形中，最优策略随成本结构变化；例如情况 1 为“两零件检测、成品不检测、拆解”，收益为 18.111111 元。

#figure(image("../../figures/problem2_sensitivity.pdf", width: 85%), caption: [情况 1 最优策略的单参数敏感性])

= 问题三：多工序生产网络

将图 1 表示为树形 DAG，叶节点为零件，内部节点为半成品或成品。对“零件和半成品均检测”的可解释候选族，从叶到根递推合格件期望成本。最佳候选为：半成品不合格时重建、最终成品不检测、售后退回后拆解复用半成品，期望净收益为 62.222222 元。该结论限于候选族，不宣称对所有节点级策略全局最优。

#figure(image("../../figures/problem3_policy_comparison.pdf", width: 85%), caption: [图 1 结构化候选策略的收益比较])

= 问题四与模型评价

若节点抽样观测为 $(x_j,n_j)$，可采用 $p_j | x_j,n_j ~ "Beta"(x_j+1/2,n_j-x_j+1/2)$ 传播不确定性。题面未提供实际观测，因此本文仅报告抽样和参数情景。模型优点是精确处理边界样本和回收循环；局限是独立性、检测准确性以及问题三候选族的限制。
