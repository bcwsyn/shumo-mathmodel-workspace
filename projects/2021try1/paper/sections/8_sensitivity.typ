= 灵敏度与稳健性分析

== 投影口径半径

工作口径的理论半径为 150 m，但离散面板重心可能靠近边界。为判断边界选择是否改变结论，在 16 阶细分下把投影口径半径取为 149.5、150.0 和 150.5 m。投影面积加权结果见 @tab:radius-sensitivity。

#figure(
  table(
    columns: (1fr, 1.25fr, 1.25fr, 1fr),
    align: center,
    stroke: none,
    table.hline(y: 0, stroke: 0.8pt),
    table.hline(y: 1, stroke: 0.5pt),
    table.header([*半径/m*], [*基准面接收比*], [*工作面接收比*], [*相对提升/%*]),
    [149.5], [0.00806220], [0.01134351], [40.70],
    [150.0], [0.00800792], [0.01126826], [40.71],
    [150.5], [0.00795501], [0.01119322], [40.71],
    table.hline(stroke: 0.8pt),
  ),
  caption: [投影口径半径敏感性],
) <tab:radius-sensitivity>

半径每增加 0.5 m，两表面的接收比均略有下降，这是新增边缘微单元的反射落点更分散所致。但三个半径下的相对提升都约为 $40.7%$，工作面优于基准面的方向不变。这里的提升率与 64 阶主结果不同，是因为灵敏度实验固定使用 16 阶细分；它只用于比较参数扰动，不替代主结果。

#figure(
  image("../figures/raytrace_sensitivity.pdf", width: 83%),
  caption: [接收比对投影口径半径的敏感性],
) <fig:radius-sensitivity>

== 能量权重

将正式的投影面积权重改为面板等权后，149.5、150.0、150.5 m 三种半径下，基准面接收比分别变为 $0.00802783$、$0.00797141$、$0.00791638$，工作面分别为 $0.01135302$、$0.01127372$、$0.01119453$。相对于投影面积加权，各接收比的绝对变化均不超过约 $4 times 10^(-5)$，且工作面仍优于基准面。这说明结论不依赖三角面板面积差异的具体权重处理。

== 优化可行性

顺序凸化从零位移、半缩放目标和最大可行缩放目标三个确定性起点运行。正式结果的最大促动器占用率为 $0.415206$，具有明显行程余量；最大主索占用率为 $0.999988950$，表明主索约束决定了最优形状。由于所有候选解都使用原始非线性几何逐项回代，求解器线性化误差不会被误认为物理可行性。

综合细分、口径和权重实验，可以确认“严格可行工作面提高接收比”这一结论具有稳定性；但主索约束接近活跃边界，若实际工程还存在未建模的安装或受力误差，应在控制应用中额外设置安全裕量。
