= 模型假设

+ *基准球心位于坐标原点。* 球面半径使用附件节点半径的均值，而不把题面中的“约 300 m”强制取整。这样能够保留附件坐标的内部一致性。

+ *节点只沿基准球心径向运动。* 促动器按基准球面径向安装，而附件没有给出索网预张力、材料刚度和节点三维自由度，因此径向位移是当前数据下可辨识且与装置结构一致的控制变量。该假设忽略了受力引起的微小切向偏移。

+ *下拉索逐根保持基准长度。* 不同节点的下拉索长度由附件几何分别恢复，不能用统一长度替代；促动器顶端沿自身基准径向移动。

+ *口径外节点固定。* 300 m 口径由基准位置对观测轴的投影圆选取。口径外节点位移为零，但所有连接活动节点的跨边界主索仍满足长度变化约束。

+ *反射面板是三顶点决定的局部平面镜。* 面板间缝隙避免相邻面板接触造成额外变形。该假设未描述面板自身柔性、表面粗糙度和装配误差。

+ *电磁波作平行直线传播。* 面板无吸收，反射服从几何光学镜面定律；暂不考虑衍射、遮挡、馈源方向图和极化。基准面与工作面使用完全一致的能量口径，因此这些忽略主要影响接收比绝对值，不改变本文的同口径比较定义。

+ *附件坐标和拓扑正确。* 对缺失字段、重复编号、未知面板节点和零面积面板均在程序入口拒绝。坐标舍入与口径选择的影响通过灵敏度实验讨论。

+ *优化结果不宣称全局最优。* 顺序凸化属于局部方法。本文采用三个确定性可行起点并逐条回代硬约束，将结果称为“多起点中的最佳严格可行解”。
