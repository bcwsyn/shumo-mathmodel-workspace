#import "../lib.typ": academic-table

= 问题二的第二层厚度设计

== 优化模型

问题二固定环境温度 $T_e=65 degree"C"$、工作终止时间 $t_f=3600 " s"$ 和第四层厚度 $d_4=5.5 " mm"$，决策变量仅为第二层厚度 $d_2$。以满足安全约束的最小厚度为目标：

$ op("min")_(d_2) quad f(d_2)=d_2 $ <eq-q2-objective>

$ "s.t." quad 0.6<=d_2<=25.0 " mm", $ <eq-q2-bound>

$ g_1(d_2)=op("max")_(0<=t<=3600)T_s(t;d_2)-47<=0, $ <eq-q2-temp>

$ g_2(d_2)=tau_44(d_2)-300<=0. $ <eq-q2-duration>

式 @eq-q2-temp 限制整个工作区间的最高温度，式 @eq-q2-duration 限制严格超过 $44 degree"C"$ 的累计时长。两项约束都由同一条完整皮肤温度曲线计算，不能只检查终点。

== 完整枚举与边界加密

将 $d_2=0.6,0.7,dots.c,25.0$ mm 的 245 个制造档位全部代入问题一标定后的传热模型。快速网格用于定位首次可行档位，随后对临界附近 15 个候选使用正式网格，并以 0.05 mm/0.5 s 细网格复核。若厚度—约束响应不单调，则保留完整枚举而不使用二分删解；本问题的峰值和临界带支持直接按厚度排序。

@fig-q2-enum 同时展示两个安全指标。随着第二层增厚，最高皮肤温度和超阈时长总体下降；在临界区域，$47 degree"C"$ 峰值约束尚有较大余量，真正决定最小厚度的是 300 s 时长约束。

#figure(
  image("../../figures/q2_enumeration.pdf", width: 90%, alt: "第二层厚度与最高皮肤温度及超过44摄氏度时长的枚举关系"),
  caption: [问题二第二层厚度枚举及安全约束],
) <fig-q2-enum>

== 临界结果与离散最小性

细网格结果见 @tab-q2-boundary。17.5 mm 的最高温度虽低于 $47 degree"C"$，但超阈时长达到 334.5 s；增至 17.6 mm 后，时长降为 293.5 s，两项约束同时满足。由于 17.5 mm 是 0.1 mm 制造精度下紧邻的更薄档位，故 17.6 mm 的离散最小性成立。

#academic-table(
  [问题二临界厚度的细网格复核],
  (6.5em, 8.5em, 8.5em, 5em),
  ([第二层厚度], [最高皮肤温度], [超过 44°C 的时长], [可行性]),
  (
    [17.5 mm], [$44.08770 degree"C"$], [334.5 s], [否],
    [*17.6 mm*], [$44.07684 degree"C"$], [293.5 s], [*是*],
    [17.7 mm], [$44.06588 degree"C"$], [252.0 s], [是],
  ),
) <tab-q2-boundary>

因此，问题二的名义设计为

$ bold(d_2^*=17.6 " mm"), quad d_4=5.5 " mm". $ <eq-q2-answer>

该方案在标定参数下的温度峰值余量为 $47-44.07684=2.92316 degree"C"$，但时长余量只有 6.5 s，属于靠近时长边界的名义最小解，不宜直接忽略参数与制造误差。

== 联合不利情景

为检验临界方案的工程风险，构造联合不利情景：$h_o$ 增加 10%、$h_b$ 降低 10%，四层导热系数增加 5%、比热容降低 5%，初温和环境温度各增加 $1 degree"C"$，第二、四层实际厚度各比计划值少 0.1 mm。所有变化均使人体侧受热更不利，因而该情景是预先定义的保守不确定集，而非由结果反向挑选。

在该情景下，对第二层厚度上限 25.0 mm 以内的全部候选重新求解，仍无法把 $tau_44$ 降至 300 s 以下。因此，问题二在题设设计范围内

#align(center)[#strong[不存在联合 10% 不利情景下的稳健可行厚度。]]

这一负结论不能用超过上限的厚度替代。若需要抵御该不利集，应同时增加第四层允许厚度、改变材料热物性，或缩短连续作业时间。
