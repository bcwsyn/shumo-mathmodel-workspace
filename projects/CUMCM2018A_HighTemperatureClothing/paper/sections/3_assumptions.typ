= 模型假设

在保留瞬态蓄热和两端换热机制的前提下，作如下假设。

+ 受热区域横向尺度远大于四层总厚度，忽略边缘、接缝、褶皱和人体曲率，只考虑沿厚度方向的一维传热。
+ 各层材料均匀、各向同性，题目给出的密度、比热容和导热系数在 $37$--$80 degree"C"$ 范围内取常数。
+ 各层紧密接触，界面温度与法向热流连续。单条皮肤温度曲线不足以同时辨识多个接触热阻，故基础模型不额外引入该类参数。
+ 第四层使用题目给出的等效导热系数，不再将空气层中的自然对流和辐射拆分为独立机制。
+ 外侧对流与未显式建模的辐射合并为等效换热系数 $h_o$；人体皮肤、假体内部换热合并为 $h_b$。
+ 进入高温环境前，四层材料与皮肤外侧的初始温度均为 $37 degree"C"$，与实验记录的初值一致。
+ 问题一标定的 $h_o,h_b$ 作为问题二、三的名义参数。环境改变可能影响等效系数，故通过 $plus.minus 10%$、$plus.minus 20%$ 扰动检查外推风险。
+ 设计厚度以 0.1 mm 为决策精度；连续临界值只用于定位，最终候选向安全方向取整并用细网格复核。

上述假设使模型保持可标定性和可计算性，但限制了结论的空间尺度与工况范围。其影响将在敏感性分析和模型评价中单独讨论。
