= 模型评价与推广

== 模型优点

+ *边界处理与观测口径一致。* 人体深部 37°C 被建模为热库而不是固定皮肤温度，双 Robin 边界能够同时解释早期升温和长期平衡；外侧辐射的未分离影响明确包含在等效 $h_o$ 中。
+ *异质界面保持守恒。* 界面对齐有限体积网格和串联热阻导通系数直接满足热流连续。能量平衡最大残差与界面热流最大失配分别为 $2.34 times 10^(-9)$ 和 $6.31 times 10^(-11) " W"/"m"^2$，与理论结构相符。
+ *数值稳定且适合大量搜索。* 全隐式格式避免显式方法的时间步稳定性限制，Thomas 预分解把每个时间步的求解复杂度降为 $O(N)$；独立厚度组合可并行，阈值积分和 Pareto 筛选可向量化。
+ *优化结论具有可复核基准。* 问题二完整枚举 245 个候选，问题三完整枚举 14,455 个组合，并用相邻档位、临界带细网格和 NSGA-II 多种子结果交叉验证。启发式算法不承担无法证明的全局最优声明。
+ *区分名义与稳健设计。* 论文同时报告名义最小厚度、联合不利情景下的稳健厚度以及无稳健可行解的情形，避免把数值边界点直接包装成无条件工程推荐。

== 模型缺点

+ 实验只有一个厚度组合和一条皮肤温度轨迹，虽然两个换热参数的 Jacobian 条件数和相关系数尚可，但没有独立环境温度或独立厚度的外部验证。
+ $h_o,h_b$ 是装置条件下的等效参数，不能分别识别辐射、空气边界层、皮肤灌注和假体内部换热；跨 65--80°C 使用依赖敏感性分析而非新增实测证据。
+ 一维模型忽略接缝、边缘、褶皱、人体曲率和局部压缩，也未描述出汗、湿度和材料热物性随温度变化。
+ 第四层按恒定等效导热系数处理，没有显式求解空气层中的自然对流和辐射；当厚度、姿态或温差进一步增大时，该近似可能偏离真实机制。
+ 临界时长对网格和阈值插值较敏感。本文通过完整枚举和细网格复核控制了当前离散误差，但没有消除物性与边界参数本身的不确定性。

== 推广

若增加多环境温度、多厚度和服装内部多测点实验，可把材料参数与边界参数分层标定，并通过留出工况检验外推能力。对第四层可进一步建立温度相关等效导热模型，或显式耦合辐射与自然对流；对接缝和人体曲率则需扩展为二维、三维有限体积或有限元模型。

本文的“瞬态机理模型—实验标定—守恒离散—低维完整搜索—稳健性分析”框架也适用于多层隔热板、冷链包装和建筑围护结构。迁移时不能直接复用本题换热系数，而应根据新的几何尺度、边界环境和观测位置重新标定。

= 结论

本文针对四层高温防护服建立了一维非稳态传热与厚度优化模型，主要结论如下。

+ 采用双 Robin 边界、界面对齐有限体积离散和全隐式时间推进，利用 Thomas 算法高效求解三对角系统。由实验曲线标定得到 $h_o=120.991689$、$h_b=8.364912 " W"/("m"^2 dot "K")$，拟合 RMSE 为 $0.002627 degree"C"$。稳态闭式解、三档网格、能量平衡和界面热流检查共同验证了问题一温度场。
+ 在 $65 degree"C"$、60 min、第四层 5.5 mm 条件下，第二层名义最小厚度为 *17.6 mm*；最高皮肤温度为 $44.07684 degree"C"$，超过 $44 degree"C"$ 的时长为 293.5 s。17.5 mm 的超阈时长为 334.5 s，证明了 0.1 mm 精度下的离散最小性。联合 10% 不利情景下，题设第二层厚度范围内不存在稳健可行解。
+ 在 $80 degree"C"$、30 min 条件下，双重枚举 14,455 个组合得到最小总厚度 25.7 mm。按同总厚度时面密度最小的规则，名义最优为 *$(d_2,d_4)=(19.3,6.4)$ mm*；20 个种子的 NSGA-II 到枚举 Pareto 前沿最大距离为 0。联合 10% 不利情景下，稳健设计为 *$(23.5,6.4)$ mm*，峰值为 $44.93951 degree"C"$、超阈时长为 294.46253 s。
+ 人体侧等效换热系数是最敏感因素。名义最优应解释为标定参数下的离散边界解，稳健方案也只对本文指定的不确定集成立。后续若以实际防护装备为目标，需要用多工况实验进一步约束边界换热和第四层空气传热机制。
