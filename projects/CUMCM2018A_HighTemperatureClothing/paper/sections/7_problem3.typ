#import "../lib.typ": academic-table

= 问题三的双层厚度优化

== 字典序优化模型

问题三的环境温度为 $80 degree"C"$、工作时长为 1800 s，第二、四层厚度同时可调。题目没有给出材料成本，若直接最小化“成本”会缺少数据依据，因此取总厚度为主目标：

$ op("min")_(d_2,d_4) quad f_1=d_2+d_4. $ <eq-q3-primary>

约束为

$ 0.6<=d_2<=25.0, quad 0.6<=d_4<=6.4 quad ("mm"), $ <eq-q3-bound>

$ op("max")_(0<=t<=1800)T_s(t;d_2,d_4)<=47 degree"C", quad
  tau_44(d_2,d_4)<=300 " s". $ <eq-q3-constraints>

若多个 0.1 mm 网格解具有相同总厚度，则以两层面密度

$ f_2=rho_2d_2+rho_4d_4 $ <eq-q3-mass>

作为第一破同规则；仍相同时选择安全余量更大的方案。该规则利用题目已给出的材料密度，不把面密度误称为未提供单价的成本。

== 双重完整枚举

第二层有 245 个离散档位，第四层有 59 个档位，因此完整搜索规模为

$ 245 times 59=14,455. $

算法按固定顺序遍历笛卡尔积。每个厚度组合重新构造界面对齐网格，预分解 Thomas 系数并计算整段皮肤温度；阈值积分、约束回代和 Pareto 筛选采用向量化数据处理，独立厚度组合可并行计算。快速枚举后，对 541 个边界候选使用 0.10 mm/1 s 网格复核，再对 202 个近优候选使用 0.05 mm/0.5 s 网格复核。

@fig-q3-region 展示了最高皮肤温度响应面与可行域。可行边界体现第二、四层厚度的替代关系，但第四层单位厚度热阻更大且密度极低，所以最小总厚度解倾向把第四层取到上限。

#figure(
  image("../../figures/q3_feasible_region.pdf", width: 94%, alt: "第二层与第四层厚度平面上的最高温度响应面、可行域和最优点"),
  caption: [问题三厚度组合的最高温度响应面与可行域],
) <fig-q3-region>

== 名义最优与破同

细网格中最小总厚度为 25.7 mm，存在 @tab-q3-tie 所列两个可行组合。二者均满足两项安全约束，但 $(19.3,6.4)$ mm 的面密度更小，故按式 @eq-q3-mass 的预设规则选为名义最优。

#academic-table(
  [问题三同总厚度候选的字典序比较],
  (5em, 5em, 6em, 7.5em, 7.2em, 7.2em),
  ([第二层], [第四层], [总厚度], [面密度], [最高温度], [超 44°C 时长]),
  (
    [*19.3 mm*], [*6.4 mm*], [*25.7 mm*], [*$16.644152 " kg"/"m"^2$*], [*$44.78600 degree"C"$*], [*293.67183 s*],
    [19.4 mm], [6.3 mm], [25.7 mm], [$16.730234 " kg"/"m"^2$], [$44.80847 degree"C"$], [298.04485 s],
  ),
) <tab-q3-tie>

名义结果为

$ bold((d_2^*,d_4^*)=(19.3,6.4) " mm"), quad
  f_1^*=25.7 " mm". $ <eq-q3-answer>

最优方案的最高皮肤温度为 $44.7859977 degree"C"$，严格超过 $44 degree"C"$ 的累计时长为 293.671831 s。与问题二相同，决定性约束仍是超阈时长；6.328169 s 的时长余量说明名义解位于可行边界附近。

== NSGA-II 交叉验证

双重枚举已经能够证明 0.1 mm 网格上的全局最优，使用随机智能算法的目的不是替代这一证据，而是验证二维非支配结构。将 $(d_2,d_4)$ 作为两个同时最小化的目标，对不可行解采用可行性优先的约束支配。NSGA-II 的非支配排序和精英保留机制见文献 [4]。

具体采用 pymoo 0.6.2，种群规模 80、迭代 80 代，随机种子取 0--19。每次遗传搜索得到的候选都映射回离散厚度，并由 PDE 求解器重新回代安全约束。@fig-q3-pareto 表明，遗传算法得到的点与完整枚举前沿重合。

#figure(
  image("../../figures/q3_pareto_cross_validation.pdf", width: 90%, alt: "完整枚举Pareto前沿与20个随机种子NSGA-II解集的交叉验证"),
  caption: [完整枚举 Pareto 前沿与 NSGA-II 交叉验证],
) <fig-q3-pareto>

20 次运行的超体积均值为 33.7418424、标准差为 0；NSGA-II 解集到枚举 Pareto 前沿的最大距离为 0，每次得到的最小总厚度均为 25.7 mm。由此可知启发式搜索稳定复现了枚举前沿，但论文中的“离散全局最优”仍由完整扫描 14,455 个组合及临界细网格复核支撑。

== 阈值时长的网格锯齿

快速网格中，最高温度随第二层增厚没有发现反向变化；然而，超阈时长出现 30 处微小反向跳变，最大增量为 1.9266 s。其原因是厚度改变时控制体数离散变化，阈值穿越又落在不同时间单元内。若据此强行使用严格单调二分，可能跳过临界候选。

本文保留完整枚举，不依赖该指标严格单调，并对所有可行边界和近优点重新使用细网格计算。因此，锯齿影响快速筛选曲线的局部光滑性，但不改变 25.7 mm 的最小总厚度和 $(19.3,6.4)$ mm 的破同结论。
